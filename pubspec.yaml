name: ower_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  flutter_rating_bar: ^4.0.1  # ← جديد للتقييم
  
  # Firebase - Compatible Versions
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  
  # Additional Packages
  shared_preferences: ^2.2.2
  url_launcher: ^6.1.10
  intl: ^0.19.0  # ← جديد للتاريخ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # assets:
  #   - images/